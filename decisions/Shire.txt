# Remove this line to have the claim get that interior arguments are correct
# Audax Validator "!" Ignore_1009

# Shire Decisions: {
#	Starting Decision (Unseen)
#	Expansion
#	Flavour }

# Starting Decision (Unseen)
political_decisions = {
	# Audax Validator "." Ignore_1004
	SHI_start_decision = {
		potential = {
			always = no
		}
		
		effect = {
			set_country_flag = flavor_fortifications_1
			set_country_flag = flavor_expansion_2
			set_country_flag = flavor_expansion_1

			# Set allowed leaders
			set_country_flag = ideologies_took_family_active
			set_country_flag = ideologies_brandybuck_family_active
			set_country_flag = ideologies_fairbairn_family_active
			set_country_flag = ideologies_underhill_family_active
			set_country_flag = nobility_deactivated
		}
	}
	BCK_start_decision = {
		potential = {
			always = no
		}
		
		effect = {
			# Set allowed leaders
			set_country_flag = ideologies_brandybuck_family_active
			set_country_flag = ideologies_brockhouse_family_active
			set_country_flag = ideologies_underhill_family_active
			set_country_flag = nobility_deactivated
		}
	}
	MAU_start_decision = {
		potential = {
			always = no
		}
		
		effect = {
			# Set allowed leaders
			set_country_flag = ideologies_brandybuck_family_active
			set_country_flag = ideologies_brockhouse_family_active
			set_country_flag = nobility_deactivated
		}
	}
	STO_start_decision = {
		potential = {
			always = no
		}
		
		effect = {
			# Set allowed leaders
			set_country_flag = ideologies_brandybuck_family_active
			set_country_flag = ideologies_brockhouse_family_active
			set_country_flag = nobility_deactivated
		}
	}
}

# Expansion
political_decisions = {
	annex_buckland = {
		picture = "brandy_hall"
		potential = {
			tag = SHI
			is_our_vassal = BCK
		}
		allow = {
			BCK = {
				all_core = {
					OR = {
						owned_by = SHI
						owned_by = BCK
					}
				}
			}
			expansion_2 = 1
			war = no
		}
		effect = {
			inherit = BCK
		}
	}
}
# Flavour
political_decisions = {
	high_hay = {
		picture = "brandy_hall"
		potential = {
			is_culture_group = hobbits
			owns = 365
			NOT = {
				365 = {
					has_building = fort
				}
			}
		}
		allow = {
			fortifications_1 = 1
			war = no
		}
		effect = {
			365 = {
				fort = 1
			}
		}
	}
}
